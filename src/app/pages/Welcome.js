import React, { Component } from 'react';
import LoginForm from '../components/LoginForm'

const Welcome = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default Welcome;